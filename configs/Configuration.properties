#URLs
base.url=https://www.google.com

# Browser settings
browser=chrome
browser.version=latest
headless=false

# Timeout settings (in seconds)
timeout.page=30
timeout.element=10

# Directory settings
screenshot.directory=target/screenshots
trace.directory=target/traces

# Other settings
retry.attempts=3